name:Keep Backend Awake

on: 
  schedule:
    - cron:"*/10 * * * *"
  workflow_dispatch:

jobs: 
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Backend
        run: curl -s https://your_backend.com/render/ping
